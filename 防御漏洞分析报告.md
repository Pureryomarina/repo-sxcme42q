# safe_bypass (天骄裸奔范围 1.4) 防御体系漏洞分析

> 分析对象：`D:\fenxiwanjian\过检加解密\完整修复\jni`
> 范围：DEFENSE.md 描述的 5 层互锁 + 5 类环境检测 + seccomp/时序/Inline Hook 增强，以及网络授权校验。
> 结论概述：架构看起来"纵深"，但 **绝大多数防御只在 `main()` 启动的约 1 秒内执行一次，且多处把"自检"用错了内核机制 / 用错了数据源**，导致实际防护强度远低于文档宣称。下面按严重程度列出可被利用的防御漏洞，并给出对应的绕过手法与修复建议。

---

## 一、致命级（直接整体失效）

### C1. 全部校验都是"一次性开机门"，周期监控被显式关闭
`main.cpp:201` 设了 `cfg.enable_periodic_monitor = false;`，Layer 0–5 + seccomp + 基线 + 时序锚点全部在 `network_verify()` 之前一次性跑完，之后程序进入 `run_draw_loop()` / `cycle_thread_func()` 持续运行，**再也不做任何完整性或环境检测**。

- 绕过：攻击者无需碰开机这 1 秒，等进程进入主循环后再 `frida -p <pid>` 附加、或 ptrace attach、或直接改内存——所有检测都已经跑完，永不复查。
- 这是整套防御最大的单点失效。`ProtectManager::Manager` 里那套周期线程（`monitor_thread_func`，2 秒一次）根本没被启用（main 走的是 `check_once` 一次调用）。
- 修复：启用周期监控线程，并在多个业务线程（绘制循环、cycle 写入线程）里插入随机间隔的 `check_once`；把检测结果喂给真正会影响功能的代码路径，而不是只在开机 `return 1`。

### C2. seccomp 用反了——它过滤的是"本进程自己"，挡不住外部调试/dump
`SeccompGuard::install()` 把 `ptrace(117)`、`process_vm_readv(270)`、`process_vm_writev(271)`、`memfd_create(279)` 等设为 `SECCOMP_RET_KILL`。DEFENSE.md 宣称"内核级阻断 ptrace/vm_read/memfd，攻击者无法在用户态绕过"。

- 关键事实：seccomp BPF 只约束**安装过滤器的进程及其子进程自己发起的 syscall**，不约束别的进程对它做什么。
  - 调试器附加时，`ptrace(PTRACE_ATTACH,...)` 是**调试器进程**发起的，受害进程的 seccomp 完全管不到。
  - 内存 dump 时，`process_vm_readv` / 打开 `/proc/<victim>/mem` 是**dumper 进程**发起的，受害进程的 seccomp 同样管不到。
- 实测佐证：本工具自己的 `mem_read/mem_write`（`draw_Gui.cpp:130-150`）走的是 `open("/proc/<game_pid>/mem")` + read/write，根本不用 `process_vm_readv`，所以这条过滤器对工具自身功能"恰好"无影响——也正说明它对攻击者一样无效。
- 实际效果：这条 seccomp 几乎只起到"禁止本进程调用这些 syscall"的作用（外加阻止本进程 `memfd_create`），对"被调试 / 被 dump"这两个它声称要防的威胁，防护值≈0，反而制造了安全错觉。
- 附带脆弱点：未在安装前 `prctl(PR_SET_NO_NEW_PRIVS,1)`（靠 root 的 CAP_SYS_ADMIN 兜底，去 root 化后会失败）；BPF 未校验 `seccomp_data.arch`（经典 32 位 compat syscall 绕过模式）；硬编码 syscall 号缺乏架构判定。
- 修复方向：anti-debug 应靠"自己 `ptrace(PTRACE_TRACEME)` 占坑 / fork 子进程反 ptrace 父进程 / 检测 TracerPid（修好 C? 见 H1）"；anti-dump 靠 `PR_SET_DUMPABLE=0`（已做）+ 持续监控谁打开了自己的 mem。seccomp 留着可以，但不要把它当 anti-debug/anti-dump 的主力。

### C3. "代码段完整性"读的是磁盘文件，从不校验内存页——纯内存 Hook/Patch 完全检测不到
DEFENSE.md 把 Layer 3/4 描述成"加载态 .text CRC / 检测内存代码段篡改"。但实现里：

- `main.cpp` Layer 3 调 `IntegrityCheck::compute_loaded_text_crc("safe_bypass")`，该函数（`integrity_sha256.cpp:232`）`readlink("/proc/self/exe")` 后 **`open` 磁盘文件**重新解析 ELF、CRC 前 90% PT_LOAD。
- `main.cpp` Layer 2 的 `compute_file_hmac` 同样是读磁盘文件。
- `ProtectManager::verify` → `verify_self_integrity` 读的也是 `/proc/self/exe` 磁盘文件。
- 真正按内存映射地址做哈希的 `IntegrityCheck::calc_text_crc / calc_text_sha256 / get_text_range`（`IntegrityCheck.h:191-227`，从 `/proc/self/maps` 取 `r-xp` 段算 CRC）**定义了却从没被调用**。

后果：所有"完整性"层只能发现**磁盘上的文件被改**，发现不了**只改内存不改磁盘**的攻击——而 Frida `Interceptor` / inline hook / 运行时 `mprotect`+patch 恰恰都是只动内存。文档与实现严重不符。
- 修复：把 Layer 4 改成对**实际加载的 `r-xp` 内存段**做 HMAC（用 `get_text_range` 那套），并与一个固定期望值（编译期或尾部）比对；磁盘校验和内存校验都要有。

### C4. 硬编码明文密钥 → 授权服务器可被完整伪造、流量可解密
`network_program.h:38-42`：
```
app_id     = "3278"
app_secret = "07210959355572403338968469716815"
rc4_key    = "MHfEkTp2nIZ6oBtMJyqvrZdRwYWzbkjn"
```
这些在 **safe_bypass（非 OLLVM 模块，见 Android.mk:35 只混淆 main.cpp/draw_Gui.cpp）** 内，`strings` / 反编译即得（elf_report 已显示 `sign`×74、`rsa`、`hash` 等遍布 .rodata）。
- 后果：拿到 `app_secret` 即可伪造 `signature=MD5(sorted+app_secret)`，拿到 `rc4_key` 即可解密/加密全部请求响应 → 直接写一个假授权服务器，离线无限发卡、跳过卡密校验、伪造心跳。整个网络授权层被一把钥匙击穿。
- 修复：密钥不可硬编码在明文段；至少放进 OLLVM 模块并运行时分段解出、用后即抹；签名改 HMAC 且密钥设备绑定；传输上 TLS + 证书锁定（见 C5）。

### C5. root 权限下的远程命令注入（curl RCE）+ 全程明文 HTTP
- 传输：`http_utils.cpp` 和 `http_check.h` 都写死 **80 端口明文 HTTP**，仅靠 RC4（密钥已泄露，见 C4）。任意中间人都能解密、篡改、重放。
- RCE：`checkVersionApi`（`network_program.h:305-307 / 325-327`）：
```cpp
snprintf(cmd, "curl -# -L %s -o %s", addr->valuestring, name->valuestring);
system(cmd);
```
`addr` / `name` 完全来自服务器响应，而进程以 **root** 运行（`main.cpp:102` 要求 `getuid()==0`）。中间人（因 C4 可任意伪造合法签名响应）只要把 `addr` 设成 `` `reboot` `` / `; rm -rf / ;` 等，即可在设备上以 root 任意命令执行；强制更新分支还会自动下载并诱导用户运行新二进制（供应链投毒）。
- 修复：禁止把服务器字符串拼进 `system()`；用 `execve` 传参数数组或内置 HTTP 下载并校验签名；改 HTTPS+pinning；下载物做签名验签后才落盘/执行。

---

## 二、高危级（单点即可击穿核心校验）

### H1. AntiDebug::detect_ptrace 永远返回 false（唯一在用的 ptrace 检测是坏的）
`AntiDebug.h:47`：
```cpp
if (strncmp(buf, "TracerPid:", 11) == 0)   // "TracerPid:" 只有 10 个字符
```
比较 11 字节会把字符串字面量的结尾 `\0` 和 `/proc` 里的 `\t` 比，**永不相等**，函数恒为 false。而 `check_once`（`ProtectManager.h:65`）里走的正是这个 `AntiDebug::detect_ptrace`；功能正常的 `RootKitDetect::detect_tracer_pid`（用 `strlen(field)`，field 不含冒号）**没有被 `check_once` 调用**。
- 后果：即使在开机那一刻，ptrace 调试也检测不到。
- 修复：长度改 10（或用 `strncmp(buf,"TracerPid",9)`）；并把 RootKit 的 tracer/vm_writev/sigstop 检测接进 `check_once`。

### H2. 所有判定都依赖 libc `memcmp`——hook 一个 memcmp 即全线放行
Layer 1/2/3/4 的最终决策全是 `memcmp(...) != 0`（main.cpp:135/183，IntegrityCheck 里 `memcmp(actual_sha, exp_sha,32)` 等）。只要把 libc `memcmp` hook 成恒返回 0，全部完整性层一起失效。`InlineHookDetect` 名单里虽含 `memcmp`，但见 H3，基线不可信。
- 修复：关键比较用自写的、内联进 OLLVM 模块的常量时间比较函数，不调用任何可被外部 hook 的导出符号。

### H3. Inline Hook 基线在运行时采集 → 开机前就 hook 的，会被当成"干净"基线
`InlineHookDetect::build_baseline()`（`main.cpp:221` 调用）在运行时对 `read/open/memcpy/memcmp/...` 取前 64 字节 CRC 当基线。若攻击者在进程起来前（典型的 Frida `spawn`/`LD_PRELOAD`/Zygisk 注入）就已 hook，这些被 hook 后的字节被当作"正常基线"记录，之后 `verify_baselines` 自然恒通过。
- 同理 `detect_single_hook` 只看入口 16 条指令的几种模式，对"改 GOT/PLT 而不改函数体"、"trampoline 在别处"的 hook 视而不见。
- 修复：基线必须是**编译期/构建期固定值**（或来自磁盘 libc 文件的预期指令），不能运行时现采。

### H4. HMAC 主密钥可从多处低成本还原
DEFENSE 把"无 key 无法重算 HMAC"当作链条基石，但：
- `patch_integrity.py:24-29` 直接以**明文**写出同一把 key（构建脚本随源码分发即泄露）。
- main.cpp 的"保护"只是单字节 XOR `^0x73`（`g_hmac_key_xor` ⊕ 0x73），强度等于没有。
- 运行期 key 解出后存于 `g_hmac_key`（.bss 明文），任何能读本进程内存的人（而 C2 表明 anti-dump 形同虚设）都能直接抠出。
- 后果：攻击者可改文件后用真 key 重算 CRC/HMAC 尾部，Layer 2/3 一起通过。
- 修复：key 设备绑定 + 运行期分段解密用后即抹 + 多轮变换；构建脚本里也不能放明文 key（从环境变量/KMS 取）。

### H5. Layer 1 自检并不能保护它声称保护的 `compute_file_hmac`
DEFENSE.md 称"替换整个 compute_file_hmac → L1 自检 → sha256 被改即暴露"。但 Layer 1 只验证 `hmac_sha256("cross_check_integrity_sha256")` 等于常量；攻击者替换的是 `compute_file_hmac` 这个**外层包装**（让它对原始未改文件计算、或直接返回期望值），根本不碰 `hmac_sha256` 本体，L1 检测不到。
- 修复：对参与决策的每个明文函数都做覆盖（或干脆把读文件+HMAC 整条放进 OLLVM 内联），不要只测最底层原语。

---

## 三、中危级（检测形同虚设 / 易规避 / 健壮性）

### M1. 大量检测是"死代码"或一次性恒假
- `TimingGuard`：需要 ≥2 个 checkpoint 才有 delta、≥3 次异常才报警，但全程只调了一次 `timing_checkpoint("startup")`（main.cpp:223）→ 永不触发。
- `AntiDump::detect_maps_modify`：`static last_crc=0`，首次调用 `last_crc==0` 直接返回 false；又只在开机调一次 → 永远 false。且按行 CRC 做 XOR 累加，与顺序无关、易碰撞。
- `RootKitDetect` 里 `detect_tracer_pid / detect_vm_writev_available / detect_sigstop_handler_removed / detect_task_directory_injection` 均未接入 `check_once`（白写）。
- `RootKitDetect::detect_libc_readable_text` 会主动把 libc 代码页 `mprotect` 成 RW 再改回——**自己制造 RWX 风险窗口**，且也没被调用。
- `AntiDump::check_so_integrity` 名为完整性检查，实则只 `stat` 看文件 `>1024` 字节，毫无校验意义。

### M2. 环境检测全是公开特征，按默认配置写死，极易规避
- Frida 仅查默认端口 27042/27043/27046/27047（`frida -D`/改端口即过）和 `gum-js-loop` 等线程名（重命名即过）。
- 模拟器只认 `Goldfish/ranchu/emulator`（改 cpuinfo 即过）。
- 注入只看 `/data/local/tmp/`、`/data/user_de/` 路径（换目录即过）。
- Magisk + Zygisk + Shamiko / 改名 Frida(gadget) 基本可绕过全部 maps/路径类检测。
- 修复：这些只能当"广撒网"的一环，需配合行为检测、服务端风控、动态特征。

### M3. loginApi 存在栈溢出与资源泄漏（健壮性+可被本地利用）
`network_program.h:348-453`：
- `char _inputKm[] = ""; scanf("%s", &_inputKm);` —— `_inputKm` 实际只有 **1 字节**，`scanf %s` 无界写入 → 栈缓冲区溢出；且 `&_inputKm` 类型也不对。
- `char card[40]; fscanf(fopen(card_path,"r"), "%s", &card);` —— `fopen` 句柄从不 `fclose`（每次登录泄漏 FD），无 NULL 检查，`%s` 无界写入 `card[40]`；imei 同理。
- 修复：用带宽度限定的格式（`%39s`）、检查/关闭文件句柄、用 `std::string`/`fgets`。

### M4. 校验失败的痕迹太明显，单一出口易被 patch
- 所有失败都打印同一句 `"[!] 程序校验失败"`，攻击者 grep 这串字符串就能在二进制里定位全部校验点，再把上游 `return 1` / `_exit` patch 掉（且这些 `return 1` 在 main 里集中、虽在 OLLVM 模块但出口单一）。
- `resCommonInit` 校验失败时还把"期望/收到的 safeCode"打印出来（信息泄露），并用 `exit(0)`（成功码退出）。
- 修复：失败不要用统一可搜索字符串；决策结果分散、延迟生效（detect 后过一段时间再以"破坏功能"的方式体现），避免集中 `return`。

### M5. 构建与加固选项偏弱
- `Android.mk` 用 `-w`（关闭所有告警，M3 那类 bug 编译期本可发现被一并屏蔽）、`-Os`，未启用 `-fstack-protector-strong` / `-D_FORTIFY_SOURCE=2` / `-Wl,-z,relro,-z,now`（NDK 默认有 PIE，但应显式确认）。
- 16MB 二进制带 471 个 dynsym 条目，符号信息利于逆向；建议 `--strip-all` / 隐藏符号 / 版本脚本。

### M6. patch_integrity 有 .py 和 .ps1 两份，存在算法漂移风险
两套补尾部脚本若 CRC/HMAC/取段逻辑不一致，会导致运行期校验误杀或留洞。建议只保留一份权威实现，另一份调用它。

---

## 四、互锁链"看起来闭合、实则可旁路"小结

DEFENSE.md 的互锁图假设"改任意一处必触发另一层"。但综合上面：

- 纯内存攻击（Frida hook / 内存 patch）：C3 表明完整性层根本不看内存；C1 表明开机后不复查；C2 表明 seccomp 不挡外部调试/dump → **整条链对运行期动态攻击几乎不设防**。
- 改磁盘文件攻击：被 Layer2/3 拦，但 H4（key 可还原）+ patch 脚本在手即可重算尾部通过。
- 判定环节：H2（hook memcmp）/ H3（基线投毒）任一即可让所有 memcmp/基线类判定放行。
- 网络授权：C4+C5 一把梭，本地离线服务器即可。

也就是说，真正能逼攻击者付出成本的，主要是 OLLVM 混淆本身（增加静态逆向难度）和"必须 root + 改磁盘要重算尾部"这两点；其余多数检测要么用错机制、要么一次性、要么是死代码。

---

## 五、优先级修复建议（按性价比）

1. **（C1）** 开启周期性复查，并把检测结果接入会真正影响功能的路径（不只是开机 return）。
2. **（C3）** 完整性校验改为对**内存 `r-xp` 段** + 磁盘文件双校验，启用已写好的 `calc_text_*`。
3. **（C4/C5）** 下线明文 `app_secret/rc4_key`，改 HMAC+设备绑定+TLS pinning；杜绝把服务器字符串拼进 `system()`。
4. **（C2）** 不再依赖 seccomp 做 anti-debug/anti-dump；改用 TraceMe 占坑 / 子进程互 ptrace / 持续监控 mem 打开者。
5. **（H1/H2/H3/H5）** 修 `detect_ptrace` 长度 bug；关键比较用内联常量时间函数；基线改编译期固定值；对每个参与决策的明文函数做覆盖。
6. **（M1–M6）** 清理死代码、修栈溢出、收敛失败出口与日志、补齐编译加固、统一 patch 脚本。
